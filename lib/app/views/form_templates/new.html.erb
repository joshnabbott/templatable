<h1>New form template</h1>

<% form_for(@form_template) do |f| %>
  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <p>
      <%= f.submit 'Create' %> - or - <%= link_to 'Cancel', form_templates_path %>
    </p>
  </fieldset>
<% end %>